const fs = require("fs");
const path = require("path");
const AbsentRecord = require("../models/absent_records");


  const batch_list = async (req, res) => {
    
  }
  module.exports = { batch_list};